var assign = require('lodash/object/assign');

function OAuth (options) {
  options = options || {};
}

assign(OAuth.prototype, {

  loginForService: function (service) {

  }

});

module.exports = OAuth;

